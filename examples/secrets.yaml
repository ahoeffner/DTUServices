# create your secrets like this.
#
# The docker container is volatile, so it will be empty after restart.
# As this file is part of the git project, never use this file for your secrest
# After copying and editing the file, run load-secrets to add the configuration.
#
# The SPRING_PROFILES_ACTIVE will determine the current environment
# Though this definition file will create <dev|test|prod>/postgres under databases,
# it should be referenced only as "postgres". The environment will automatically be applied.
#
# All types of secrets can be defined and referenced using the library class Secrets
# but as stated, always include the environment as the second level in the definitions.
#
# Secrets are also available from the java library. Just use @Autowired Secrets
#

secrets:
  databases:
      postgres:
        dev:
            username: postgres
            password: Manager1
        test:
            username: postgres
            password: Manager1
        prod:
            username: postgres
            password: Manager1

  oauth2:
    fusion:
      dev:
        client-id: "8d61a81587924a75af305021ff15ff67"
        client-secret: "idcscs-f405d9da-cec5-4c8f-9609-cf378ed3519b"
      test:
        client-id: "8cc619d9660047ffa7e8bcd3823da9ee"
        client-secret: "idcscs-b3c94bfe-88d1-481c-84a4-1d47822800fe"
      prod:
        client-id: "8cc619d9660047ffa7e8bcd3823da9ee"
        client-secret: "idcscs-b3c94bfe-88d1-481c-84a4-1d47822800fe"
